---
import PageLayout from "../layouts/PageLayout.astro";
import LocalBusinessSchema from "../components/schema/LocalBusinessSchema.astro";
import Container from "../components/ui/Container.astro";
import { getCollection } from "astro:content";

const allTestimonials = (await getCollection("testimonials")).sort((a, b) =>
  a.id.localeCompare(b.id)
);
const allFaqs = await getCollection("faqs");
const sortedFaqs = allFaqs.sort((a, b) => a.data.order - b.data.order);

const siteSettingsCollection = await getCollection("siteSettings");
const site = siteSettingsCollection[0]?.data;

const homepageCollection = await getCollection("homepageSettings");
const hp = homepageCollection[0]?.data;
---

<PageLayout
  title={hp?.metaTitle ?? "Millie The Hut | Shepherd's Hut Glamping in the Welsh Marches, Herefordshire"}
  description={hp?.metaDescription ?? "Escape to Millie The Hut — a beautifully appointed shepherd's hut nestled in the Welsh Marches near Presteigne. Luxury glamping for two with a wood burner, fire pit & stunning countryside views."}
>
  <Fragment slot="head">
    <LocalBusinessSchema />
  </Fragment>

  <h1 class="sr-only">Millie The Hut — Shepherd's Hut Glamping in the Welsh Marches</h1>

  <!-- Hero -->
  <section class="hero" aria-label="Hero">
    <div class="hero__frame">
      <img
        src="/images/hero.avif"
        alt="Millie The Hut shepherd's hut at golden hour in the Welsh Marches countryside"
        class="hero__img"
        fetchpriority="high"
        decoding="async"
      />
      <div class="hero__overlay">
        <div class="hero__overlay-top"></div>
        <p class="hero__tagline" set:html={(hp?.heroOverlayText ?? "Escape to the\nWelsh Marches").replace('\n', '<br />')}></p>
      </div>
    </div>
  </section>

  <!-- Introduction -->
  <section class="section section--white" aria-labelledby="intro-heading">
    <Container narrow>
      <div class="intro">
        <h2 class="intro__heading" id="intro-heading">{hp?.introHeading ?? "Millie The Hut, Presteigne, Herefordshire."}</h2>
        <div class="intro__text" id="scroll-text">
          <p>{hp?.introParagraph ?? "Down a leafy lane, where steam trains once ran, across a Victorian railway bridge that takes you from Wales to England, you'll find a meadow where sheep graze, and a traditional-style Shepherds Hut and Shack. On sunny days, the meadow is bathed in light from dawn to dusk, and the gin-clear Hindwell Brook that bounds it flashes silver. Whatever the weather, after dark is a special time to be here, when the log burner in the hut, the firepit by the stream and the lamps and candles in the Shack cast a magical golden glow."}</p>
        </div>
      </div>
    </Container>
  </section>

  <!-- Trust Indicators -->
  <section class="section section--white section--tight-top" aria-labelledby="trust-heading">
    <Container>
      <h2 class="sr-only" id="trust-heading">Why Choose Millie The Hut</h2>
      <div class="trust" data-reveal-stagger>
        <div class="trust__item">
          <img class="trust__img trust__img--bg-remove" src="/images/i.jpg" alt="Independent award — one of the best 25 budget glamping sites in the UK" width="150" height="150" loading="lazy" decoding="async" />
          <p class="trust__label">One of the best 25 'budget' glamping sites in the UK</p>
        </div>
        <div class="trust__item">
          <img class="trust__img" src="/images/dt.png" alt="Distinctly Norfolk award logo" width="150" height="150" loading="lazy" decoding="async" />
          <p class="trust__label">"Remarkably affordable.... excellent choice"</p>
        </div>
        <div class="trust__item">
          <img class="trust__img" src="/images/luxlife.png" alt="Lux Life award logo" width="150" height="150" loading="lazy" decoding="async" />
          <p class="trust__label">Best Shepherds Hut UK 2025</p>
        </div>
      </div>
    </Container>
  </section>

  <!-- Animated Line Divider -->
  <div class="section--white">
    <Container>
      <div class="line-divider" data-line-divider></div>
    </Container>
  </div>

  <!-- Gallery Preview -->
  <section class="section section--white" aria-labelledby="gallery-heading">
    <Container>
      <h2 class="section__heading" id="gallery-heading">{hp?.galleryPreviewHeading ?? "Gallery"}</h2>
      <div class="gallery-preview" data-reveal="fade">
        <div class="gallery-preview__left">
          <div class="gallery-preview__img-wrap gallery-preview__img-wrap--portrait">
            <img src="/images/stars.avif" alt="Starry night sky over Millie The Hut in the Welsh Marches" class="gallery-preview__img" loading="lazy" decoding="async" />
          </div>
        </div>
        <div class="gallery-preview__right">
          <div class="gallery-preview__img-wrap">
            <img src="/images/bbq.jpg" alt="Kadai fire pit and barbecue at Millie The Hut" class="gallery-preview__img" loading="lazy" decoding="async" />
          </div>
          <div class="gallery-preview__img-wrap">
            <img src="/images/drone.jpg" alt="Aerial view of Millie The Hut and surrounding countryside" class="gallery-preview__img" loading="lazy" decoding="async" />
          </div>
        </div>
      </div>
      <div class="gallery-preview__cta">
        <a href="/gallery/" class="btn">View All <span class="btn__arrow">&rarr;</span></a>
      </div>
    </Container>
  </section>

  <!-- Animated Line Divider -->
  <div class="section--cream">
    <Container>
      <div class="line-divider" data-line-divider></div>
    </Container>
  </div>

  <!-- Facilities -->
  <section class="section section--cream" aria-labelledby="facilities-heading">
    <Container>
      <h2 class="section__heading" id="facilities-heading">{hp?.facilitiesHeading ?? "The Facilities"}</h2>
      <ul class="facilities" data-reveal-stagger>
        {(hp?.facilities ?? [
          "En suite shower & flushing toilet",
          "Bedding, towels & toiletries",
          "Eggs, milk, tea, coffee, oils etc",
          "Induction hob, microwave, toaster, kettle",
          "Fridge with ice box",
          "USB points",
          "Wood burner",
          "Kadai fire pit & barbecue",
          "Bluetooth radio & board games",
        ]).map((item) => (
          <li class="facilities__item">
            <svg class="facilities__icon" xmlns="http://www.w3.org/2000/svg" width="36" height="36" viewBox="0 0 256 256" fill="currentColor"><path d="M229.66,77.66l-128,128a8,8,0,0,1-11.32,0l-56-56a8,8,0,0,1,11.32-11.32L96,188.69,218.34,66.34a8,8,0,0,1,11.32,11.32Z"/></svg>
            <span>{item}</span>
          </li>
        ))}
      </ul>
    </Container>
  </section>

  <!-- Animated Line Divider -->
  <div class="section--cream">
    <Container>
      <div class="line-divider" data-line-divider></div>
    </Container>
  </div>

  <!-- The Essentials -->
  <section class="section section--cream" aria-labelledby="essentials-heading">
    <Container>
      <h2 class="section__heading" id="essentials-heading">{hp?.essentialsHeading ?? "The Essentials"}</h2>
      <div class="essentials" data-reveal-stagger>
        {(hp?.essentials ?? [
          { label: "Sleeps Two", sub: "No babies or children" },
          { label: "Check in times:", sub: "In: 3pm | Out: 10am" },
          { label: "No Pets", sub: "Sorry!" },
        ]).map((item) => (
          <div class="essentials__item">
            <p class="essentials__label"><strong>{item.label}</strong></p>
            <p class="essentials__sub">{item.sub}</p>
          </div>
        ))}
      </div>
    </Container>
  </section>

  <!-- Testimonials -->
  <section class="section section--white" aria-labelledby="testimonials-heading">
    <Container>
      <h2 class="section__heading" id="testimonials-heading">{hp?.testimonialsHeading ?? "What Our Guests Say"}</h2>
      <div class="testimonials-wrap">
        <div class="testimonials-scroll" id="testimonials-scroll">
          {allTestimonials.length > 0 ? (
            allTestimonials.map((t) => (
              <blockquote class="review-card">
                <span class="review-card__quote" aria-hidden="true">&ldquo;</span>
                <p class="review-card__text">{t.body}</p>
                <button class="review-card__toggle" type="button" aria-expanded="false">Read more</button>
                <footer class="review-card__footer">
                  <span class="review-card__name">{t.data.name}</span>
                  <span class="review-card__date">{t.data.date}</span>
                </footer>
              </blockquote>
            ))
          ) : (
            <>
              <blockquote class="review-card">
                <span class="review-card__quote" aria-hidden="true">&ldquo;</span>
                <p class="review-card__text">The most magical place we've ever stayed. Waking up to the sound of the brook and the sheep grazing in the meadow was absolutely heavenly. The hut has everything you need and more — we didn't want to leave.</p>
                <button class="review-card__toggle" type="button" aria-expanded="false">Read more</button>
                <footer class="review-card__footer">
                  <span class="review-card__name">Sarah &amp; James</span>
                  <span class="review-card__date">February 2026</span>
                </footer>
              </blockquote>
              <blockquote class="review-card">
                <span class="review-card__quote" aria-hidden="true">&ldquo;</span>
                <p class="review-card__text">A truly special retreat. The attention to detail is extraordinary — from the wood burner to the fire pit by the stream, every moment felt considered. The perfect escape from city life.</p>
                <button class="review-card__toggle" type="button" aria-expanded="false">Read more</button>
                <footer class="review-card__footer">
                  <span class="review-card__name">Emily</span>
                  <span class="review-card__date">January 2026</span>
                </footer>
              </blockquote>
            </>
          )}
        </div>
        <button class="testimonials-arrow" type="button" aria-label="Scroll to next review" id="testimonials-next">
          &rarr;
        </button>
      </div>
    </Container>
  </section>

  <!-- Location & Map -->
  <section class="section section--white" aria-labelledby="location-heading">
    <Container>
      <h2 class="section__heading" id="location-heading">{hp?.locationHeading ?? "Where to Find Us"}</h2>
      <div class="location">
        <div class="location__info">
          <div class="location__w3w">
            <h3 class="location__sub-heading">{hp?.w3wHeading ?? "What3Words"}</h3>
            <p class="location__words">{hp?.w3wAddress ?? "///unwanted.passports.among"}</p>
            <p class="location__w3w-explain">{hp?.w3wDescription ?? "What3Words divides the world into 3m squares, each with a unique three-word address. Use it for accurate navigation directly to Millie The Hut."}</p>
          </div>
          <address class="location__address" set:html={(hp?.displayAddress ?? "Wegnall's Mill\nPresteigne\nPowys\nLD8 2LD").replace(/\n/g, '<br />')}></address>
        </div>
        <div class="location__map">
          <iframe
            src="https://www.google.com/maps/embed?pb=!1m14!1m12!1m3!1d2500!2d-2.995298!3d52.261831!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!5e0!3m2!1sen!2suk"
            width="100%"
            height="400"
            style="border:0; border-radius: var(--radius-xl);"
            allowfullscreen=""
            loading="lazy"
            referrerpolicy="no-referrer-when-downgrade"
            title="Map showing location of Millie The Hut near Presteigne, Powys"
          ></iframe>
        </div>
      </div>
    </Container>
  </section>

  <!-- FAQ -->
  <section class="section section--cream" aria-labelledby="faq-heading">
    <Container narrow>
      <h2 class="section__heading" id="faq-heading">{hp?.faqHeading ?? "Frequently Asked Questions"}</h2>
      <div class="faq">
        {sortedFaqs.length > 0 ? (
          sortedFaqs.map((faq) => (
            <details class="faq__item">
              <summary class="faq__question">{faq.data.question}</summary>
              <p class="faq__answer" set:html={faq.data.answer}></p>
            </details>
          ))
        ) : (
          <>
            <details class="faq__item">
              <summary class="faq__question">What should I pack for a Shepherd's Hut stay in the Welsh Marches?</summary>
              <p class="faq__answer">We provide bedding, towels and toiletries so you can travel light. We'd recommend bringing walking boots or wellies, warm layers for evenings by the fire pit, and a torch. Everything else you need — from tea and coffee to cooking essentials — is already here waiting for you.</p>
            </details>
            <details class="faq__item">
              <summary class="faq__question">Is there Wi-Fi at the shepherd's hut?</summary>
              <p class="faq__answer">There is no Wi-Fi at Millie The Hut — and that's by design. This is a place to disconnect and recharge. Mobile signal varies by network but is generally available. We find most guests welcome the chance to switch off.</p>
            </details>
            <details class="faq__item">
              <summary class="faq__question">Is parking available at the glamping site?</summary>
              <p class="faq__answer">Yes, free private parking is available right next to the hut. You can unload directly from your car before parking up for your stay.</p>
            </details>
            <details class="faq__item">
              <summary class="faq__question">Is the shepherd's hut suitable for a winter stay?</summary>
              <p class="faq__answer">Absolutely. The wood burner keeps the hut beautifully warm and cosy, making winter one of the most magical times to visit. There's nothing quite like cosying up by the fire while frost sparkles on the meadow outside.</p>
            </details>
            <details class="faq__item">
              <summary class="faq__question">Are there pubs, restaurants or walks nearby?</summary>
              <p class="faq__answer">Presteigne is just a short drive away and has excellent pubs, cafés and an award-winning independent cinema. The surrounding countryside offers stunning walks along Offa's Dyke, through the Radnorshire hills, and along the River Lugg. We provide a local guide with all our favourite recommendations.</p>
            </details>
            <details class="faq__item">
              <summary class="faq__question">How do I find Millie The Hut using What3Words?</summary>
              <p class="faq__answer">Our What3Words address is ///unwanted.passports.among. Simply enter this into the What3Words app or website and it will navigate you directly to within three metres of the hut entrance. It's the most accurate way to find us down our leafy lane.</p>
            </details>
          </>
        )}
      </div>
    </Container>
  </section>

</PageLayout>

<style>
  /* ─── Section Backgrounds ─── */
  .section--cream {
    background-color: var(--color-bg);
  }
  .section--white {
    background-color: var(--color-bg-white);
  }

  /* ─── Animated Line Divider ─── */
  .line-divider {
    height: 1px;
    background: rgba(0, 0, 0, 0.15);
    transform: scaleX(0);
    transform-origin: center;
    transition: transform 1.2s var(--ease-out);
  }
  .line-divider.is-animating {
    transform: scaleX(1);
  }

  /* ─── Hero ─── */
  .hero {
    min-height: 100vh;
    padding: var(--hero-padding);
    display: flex;
  }

  .hero__frame {
    position: relative;
    flex: 1;
    border-radius: var(--radius-hero);
    overflow: hidden;
  }

  .hero__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    inset: 0;
  }

  .hero__overlay {
    position: relative;
    z-index: 1;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: var(--space-xl) var(--container-padding);
    min-height: calc(100vh - var(--hero-padding) * 2);
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.25) 0%,
      transparent 40%,
      transparent 60%,
      rgba(0, 0, 0, 0.3) 100%
    );
  }

  .hero__tagline {
    font-family: var(--font-display);
    font-size: 48px;
    font-style: italic;
    color: var(--color-white);
    align-self: flex-end;
    text-align: right;
    max-width: 20ch;
    letter-spacing: var(--tracking-tight);
    line-height: 1.15;
  }

  /* Mobile: 16:11 image ratio with tagline overlay */
  @media (max-width: 47.999rem) {
    .hero {
      min-height: auto;
    }
    .hero__frame {
      aspect-ratio: 16 / 11;
    }
    .hero__overlay {
      display: flex;
      min-height: 0;
      height: 100%;
      padding: var(--space-md) var(--space-lg);
      background: linear-gradient(180deg, transparent 55%, rgba(0,0,0,0.35) 100%);
    }
    .hero__overlay-top {
      display: none;
    }
    .hero__tagline {
      font-size: 22px;
    }
  }

  @media (min-width: 48rem) {
    .hero__tagline {
      align-self: flex-end;
    }
  }

  /* ─── Sections ─── */
  .section {
    padding: var(--section-padding) 0;
  }

  .section__heading {
    font-family: var(--font-display);
    font-size: 50px;
    font-weight: var(--weight-bold);
    text-align: center;
    margin-bottom: var(--space-3xl);
    letter-spacing: var(--tracking-tight);
  }

  /* ─── Introduction ─── */
  .intro {
    text-align: center;
  }

  .intro__heading {
    font-family: var(--font-display);
    font-size: 50px;
    font-weight: var(--weight-bold);
    margin-top: 40px;
    margin-bottom: var(--space-2xl);
    letter-spacing: var(--tracking-tight);
  }

  .intro__text p {
    font-family: var(--font-display);
    font-size: 22px;
    line-height: var(--leading-relaxed);
    max-width: 60ch;
    margin-inline: auto;
    color: #cccccc;
    transition: color 0.1s linear;
    letter-spacing: var(--tracking-tight);
  }

  .intro__text p.is-revealed {
    color: var(--color-text);
  }

  .section--tight-top {
    padding-top: calc(var(--section-padding) / 3);
  }

  /* ─── Trust Indicators ─── */
  .trust {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-2xl);
    text-align: center;
    max-width: 56rem;
    margin-inline: auto;
  }

  .trust__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
  }

  .trust__img {
    width: 150px;
    height: 150px;
    object-fit: contain;
  }

  .trust__img--bg-remove {
    mix-blend-mode: multiply;
  }

  .trust__label {
    font-family: var(--font-sans);
    font-size: 17px;
    font-weight: var(--weight-bold);
    letter-spacing: var(--tracking-tight);
  }

  @media (min-width: 48rem) {
    .trust {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* ─── Gallery Preview ─── */
  .gallery-preview {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-md);
  }

  .gallery-preview__right {
    display: grid;
    gap: var(--space-md);
  }

  .gallery-preview__img-wrap {
    border-radius: var(--radius-xl);
    overflow: hidden;
  }

  .gallery-preview__img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform var(--duration-slow) var(--ease-out);
  }

  .gallery-preview__img-wrap:hover .gallery-preview__img {
    transform: scale(1.03);
  }

  .gallery-preview__cta {
    text-align: center;
    margin-top: var(--space-2xl);
  }

  @media (min-width: 48rem) {
    .gallery-preview {
      grid-template-columns: 45% 1fr;
    }
    .gallery-preview__img-wrap--portrait {
      height: 100%;
    }
    .gallery-preview__img-wrap--portrait img {
      height: 100%;
    }
  }

  /* ─── Button ─── */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    padding: 0.75rem 2rem;
    background: var(--color-cta);
    color: var(--color-black);
    font-family: var(--font-sans);
    font-size: 15px;
    font-weight: var(--weight-bold);
    border-radius: var(--radius-btn);
    transition: transform var(--duration-fast) var(--ease-out),
                box-shadow var(--duration-fast) var(--ease-out);
    letter-spacing: var(--tracking-tight);
  }

  .btn__arrow {
    display: inline-block;
    transition: transform var(--duration-fast) var(--ease-out);
  }

  .btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.12);
  }

  .btn:hover .btn__arrow {
    transform: translateX(4px);
  }

  /* ─── Facilities ─── */
  .facilities {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-2xl) var(--space-xl);
    max-width: 56rem;
    margin-inline: auto;
  }

  .facilities__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    gap: var(--space-sm);
  }

  .facilities__icon {
    color: var(--color-primary);
    flex-shrink: 0;
  }

  .facilities__item span {
    font-family: var(--font-sans);
    font-size: 17px;
    line-height: var(--leading-snug);
    letter-spacing: var(--tracking-tight);
  }

  @media (min-width: 48rem) {
    .facilities {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* ─── The Essentials ─── */
  .essentials {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-2xl);
    text-align: center;
    max-width: 56rem;
    margin-inline: auto;
  }

  .essentials__item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-md);
  }

  .essentials__icon {
    color: var(--color-primary);
  }

  .essentials__label {
    font-family: var(--font-sans);
    font-size: 17px;
    font-weight: var(--weight-normal);
    letter-spacing: var(--tracking-tight);
  }

  .essentials__label strong {
    font-weight: var(--weight-bold);
  }

  .essentials__sub {
    font-family: var(--font-sans);
    font-size: 17px;
    color: var(--color-text-muted);
    letter-spacing: var(--tracking-tight);
  }

  @media (min-width: 48rem) {
    .essentials {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  /* ─── Testimonials — Horizontal Scroll Cards ─── */
  .testimonials-wrap {
    position: relative;
  }

  .testimonials-scroll {
    display: flex;
    gap: var(--space-lg);
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    -webkit-overflow-scrolling: touch;
    padding-bottom: var(--space-md);
    scrollbar-width: none;
    scroll-behavior: smooth;
  }

  .testimonials-scroll::-webkit-scrollbar {
    display: none;
  }

  .review-card {
    flex: 0 0 clamp(280px, 38vw, 400px);
    scroll-snap-align: start;
    background: var(--color-bg);
    border: 1px solid var(--color-border-light);
    border-radius: var(--radius-xl);
    padding: var(--space-2xl);
    display: flex;
    flex-direction: column;
    min-height: 220px;
  }

  .review-card__quote {
    font-family: var(--font-display);
    font-size: 4rem;
    line-height: 1;
    color: var(--color-primary);
    display: block;
    margin-bottom: calc(-1 * var(--space-md));
  }

  .review-card__text {
    font-family: var(--font-sans);
    font-size: 15px;
    line-height: var(--leading-relaxed);
    flex: 1;
    letter-spacing: var(--tracking-tight);
    display: -webkit-box;
    -webkit-line-clamp: 5;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .review-card__text--expanded {
    display: block;
    overflow: visible;
    -webkit-line-clamp: unset;
  }

  .review-card__toggle {
    background: none;
    border: none;
    padding: 0;
    margin-top: var(--space-sm);
    font-family: var(--font-sans);
    font-size: 13px;
    font-weight: var(--weight-bold);
    color: var(--color-primary);
    cursor: pointer;
    letter-spacing: var(--tracking-tight);
    display: none;
  }

  .review-card__toggle:hover {
    text-decoration: underline;
  }

  .review-card__footer {
    margin-top: var(--space-xl);
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
    border-top: 1px solid var(--color-border-light);
    padding-top: var(--space-md);
  }

  .review-card__name {
    font-family: var(--font-sans);
    font-weight: var(--weight-bold);
    font-size: 15px;
    letter-spacing: var(--tracking-tight);
  }

  .review-card__date {
    font-family: var(--font-sans);
    font-size: 13px;
    color: var(--color-text-muted);
    letter-spacing: var(--tracking-tight);
  }

  .testimonials-arrow {
    position: absolute;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 3rem;
    height: 3rem;
    border-radius: var(--radius-full);
    border: 1px solid var(--color-border);
    background: var(--color-white);
    cursor: pointer;
    font-size: 1.25rem;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-md);
    transition: transform var(--duration-fast) var(--ease-out),
                box-shadow var(--duration-fast) var(--ease-out);
    z-index: 2;
  }

  .testimonials-arrow:hover {
    transform: translateY(-50%) scale(1.05);
    box-shadow: var(--shadow-lg);
  }

  /* ─── Location ─── */
  .location {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-3xl);
  }

  .location__sub-heading {
    font-family: var(--font-sans);
    font-size: 17px;
    font-weight: var(--weight-bold);
    margin-bottom: var(--space-sm);
    letter-spacing: var(--tracking-tight);
  }

  .location__words {
    font-family: var(--font-sans);
    font-size: 17px;
    font-weight: var(--weight-bold);
    color: var(--color-primary);
    margin-bottom: var(--space-md);
    letter-spacing: var(--tracking-tight);
  }

  .location__w3w-explain {
    font-family: var(--font-sans);
    font-size: 17px;
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
    max-width: 45ch;
    margin-bottom: var(--space-2xl);
    letter-spacing: var(--tracking-tight);
  }

  .location__address {
    font-family: var(--font-sans);
    font-style: normal;
    font-size: 17px;
    line-height: var(--leading-relaxed);
    letter-spacing: var(--tracking-tight);
  }

  .location__map {
    border-radius: var(--radius-xl);
    overflow: hidden;
  }

  .location__map iframe {
    display: block;
  }

  @media (min-width: 48rem) {
    .location {
      grid-template-columns: 1fr 1.5fr;
      align-items: start;
    }
  }

  /* ─── FAQ ─── */
  .faq {
    max-width: 48rem;
    margin-inline: auto;
  }

  .faq__item {
    border-bottom: 1px solid var(--color-border-light);
    padding: var(--space-lg) 0;
  }

  .faq__question {
    cursor: pointer;
    font-family: var(--font-display);
    font-weight: var(--weight-semibold);
    font-size: 22px;
    list-style: none;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--space-md);
    letter-spacing: var(--tracking-tight);
  }

  .faq__question::-webkit-details-marker {
    display: none;
  }

  .faq__question::after {
    content: "+";
    font-size: var(--text-xl);
    font-weight: var(--weight-normal);
    flex-shrink: 0;
    transition: transform var(--duration-fast) var(--ease-out);
  }

  .faq__item[open] .faq__question::after {
    transform: rotate(45deg);
  }

  .faq__answer {
    margin-top: var(--space-md);
    font-family: var(--font-sans);
    font-size: 17px;
    line-height: var(--leading-relaxed);
    color: var(--color-text-muted);
    max-width: 60ch;
    letter-spacing: var(--tracking-tight);
  }
</style>

<script is:inline>
  // ─── Testimonial Arrow Scroll ───
  (function () {
    var scrollContainer = document.getElementById("testimonials-scroll");
    var nextBtn = document.getElementById("testimonials-next");
    if (!scrollContainer || !nextBtn) return;

    nextBtn.addEventListener("click", function () {
      var cards = scrollContainer.querySelectorAll(".review-card");
      if (!cards.length) return;
      var cardWidth = cards[0].offsetWidth + 24; // card width + gap
      scrollContainer.scrollBy({ left: cardWidth, behavior: "smooth" });
    });
  })();

  // ─── Review Card Read More Toggle ───
  (function () {
    document.querySelectorAll(".review-card").forEach(function (card) {
      var textEl = card.querySelector(".review-card__text");
      var btn = card.querySelector(".review-card__toggle");
      if (!textEl || !btn) return;

      if (textEl.scrollHeight > textEl.clientHeight) {
        btn.style.display = "block";
      }

      btn.addEventListener("click", function () {
        var expanded = textEl.classList.toggle("review-card__text--expanded");
        btn.textContent = expanded ? "Show less" : "Read more";
        btn.setAttribute("aria-expanded", String(expanded));
      });
    });
  })();

  // ─── Scroll Text Reveal ───
  (function () {
    if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

    var el = document.getElementById("scroll-text");
    if (!el) return;
    var p = el.querySelector("p");
    if (!p) return;

    var text = p.textContent;
    var words = text.split(/(\s+)/);
    p.innerHTML = "";
    var spans = [];

    words.forEach(function (word) {
      if (word.match(/^\s+$/)) {
        p.appendChild(document.createTextNode(word));
      } else {
        var span = document.createElement("span");
        span.textContent = word;
        span.style.color = "#cccccc";
        span.style.transition = "color 0.3s ease";
        p.appendChild(span);
        spans.push(span);
      }
    });

    function updateColors() {
      var rect = el.getBoundingClientRect();
      var windowH = window.innerHeight;
      var elTop = rect.top;

      // Animation triggers when text is 15% down the page
      var triggerTop = windowH * 0.85;
      var triggerEnd = windowH * 0.15;
      var progress = Math.max(0, Math.min(1, (triggerTop - elTop) / (triggerTop - triggerEnd)));

      var revealCount = Math.floor(progress * spans.length);
      spans.forEach(function (span, i) {
        span.style.color = i < revealCount ? "#000000" : "#cccccc";
      });
    }

    var ticking = false;
    window.addEventListener("scroll", function () {
      if (!ticking) {
        requestAnimationFrame(function () {
          updateColors();
          ticking = false;
        });
        ticking = true;
      }
    }, { passive: true });

    updateColors();
  })();

  // ─── Line Divider Animation ───
  (function () {
    if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

    var dividers = document.querySelectorAll("[data-line-divider]");
    if (!dividers.length) return;

    var observer = new IntersectionObserver(function (entries) {
      entries.forEach(function (entry) {
        if (entry.isIntersecting) {
          var el = entry.target;
          observer.unobserve(el);
          setTimeout(function () {
            el.classList.add("is-animating");
          }, 1500);
        }
      });
    }, { threshold: 0.1 });

    dividers.forEach(function (d) { observer.observe(d); });
  })();
</script>
